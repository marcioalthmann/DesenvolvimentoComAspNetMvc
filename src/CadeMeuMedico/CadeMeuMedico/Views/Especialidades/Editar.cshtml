@model CadeMeuMedico.Models.Especialidades

@{
    ViewBag.Title = "Editar";
}

<h2>Editar Especialidade</h2>

@using (Html.BeginForm("Editar", "Especialidades", FormMethod.Post, new{@class = "form-horizontal"})) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Dados da especialidade</legend>
        @Html.HiddenFor(model => model.IDEspecialidade)
        <div class="editor-label">
            @Html.LabelFor(model => model.Nome)
        </div>
        <div class="editor-field">
            @Html.TextBoxFor(model => model.Nome, new{@class="form-control"})
            @Html.ValidationMessageFor(model => model.Nome)
        </div>
        <br />
        <p>
            <input type="submit" value="Gravar" class="btn btn-primary" />
        </p>
    </fieldset>
}

@section Scripts {
    <script>
        $("#menuEspecialidades").addClass("active");
    </script>
}
